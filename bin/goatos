#!/usr/bin/env ruby
# vim: set ft=ruby:

require 'mixlib/cli'
require 'blender'
require 'goatos/cli'


cli = GoatOS::CLI.new
cli.parse_options
case cli.cli_arguments.first
when 'bootstrap'
  cli.bootstrap
when 'cleanse'
  cli.cleanse
else
  abort 'Unknown goatos command'
end
